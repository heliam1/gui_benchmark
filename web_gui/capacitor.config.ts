import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.heliam1.gui_benchmark',
  appName: 'web_gui',
  webDir: 'build/web',
  bundledWebRuntime: false
};

export default config;
